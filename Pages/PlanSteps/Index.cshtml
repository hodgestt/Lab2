@* Nick Patterson & Jessica Shamloo *@

@page "{PlanName}/{PlanID:int}"
@using System.Data.SqlClient
@using Lab2.Pages.DB
@model Lab2.Pages.PlanSteps.IndexModel
@{
    ViewData["Title"] = "PlanSteps";
}

<h2 class="h2">@RouteData.Values["PlanName"] Plan Steps</h2>

<table class="table table-bordered table-responsive table-striped table-hover">
    <thead >
        <tr>
            <th>Step Description:</th>
            <th>Status:</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var p in Model.PlanStepsTable)
            {
                <tr>
                    <td>@p.StepDescription</td>
                    <td>@p.Status</td>
                </tr>
            }

            DBClass.Lab2DBConnection.Close();
        }

    </tbody>
</table>
<br/>

<br />
<br />
<a asp-page="/Plan/Index" style="-webkit-text-fill-color:rebeccapurple;">Return to Plans</a>
<a asp-page="/PlanSteps/AddPlanStep" class="btn" style="background-color:rebeccapurple; -webkit-text-fill-color:white;" asp-route-planId="@Model.PlanID" asp-route-planName="@Model.PlanName">Add Steps</a>
